/**
 * This file is retired. Firebase has been replaced with Supabase + Cloudflare R2.
 * All auth and DB operations now use @/lib/supabase and @/services/db.
 *
 * Kept as an empty shim to prevent stale import errors during the transition.
 */
export {};
